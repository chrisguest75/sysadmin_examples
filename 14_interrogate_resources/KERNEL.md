# KERNEL

```sh
# distro info
lsb_release -a

# kernel version
uname -a

# print out kernel compile options 
zcat /proc/config.gz

# lsmod is a trivial program which nicely formats the contents of the /proc/modules, showing what kernel modules are currently loaded.
lsmod
```

## Resources
